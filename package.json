{
  "name": "shramsahyog-app",
  "version": "1.0.0",
  "private": true,
  "description": "ShramSahyog — cooperative gig services demo (static site, no build step).",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node test.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
